import styled from "styled-components";

// Aside is just like div, use anything, doesn't matter.
export const Wrapper = styled.aside`
  font-family: Arial, Helvetica, sans-serif;
  width: 25rem;
  padding: 1.2rem;
`;
